import ComingSoon from "@/components/common/iconAndText/ComingSoon";

const Page = () => {
  const text =
    "Siden for å kjøpe produkter er under utvikling, og kommer snart!";
  return <ComingSoon text={text} />;
};

export default Page;
